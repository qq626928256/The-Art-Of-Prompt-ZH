<!--
name: 'System Reminder: Agent mention'
description: Notification that user wants to invoke an agent
ccVersion: 2.1.18
variables:
  - ATTACHMENT_OBJECT
-->
用户表示想要调用 agent "${ATTACHMENT_OBJECT.agentType}"。请适当调用该 agent，向其传递所需的上下文。

